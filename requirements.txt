# Core LLM + LangChain ecosystem
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0

# Groq Cloud Integration (replaces OpenAI)
langchain-groq>=0.1.0
groq>=0.4.0

# Tavily Search Tool
tavily-python>=0.3.0

# Web scraping
beautifulsoup4>=4.12.0
requests>=2.31.0
lxml>=5.0.0

# Environment variables
python-dotenv>=1.0.0

# Async support (important for agents/tools)
aiohttp>=3.9.0

# Data handling (optional but useful in pipelines)
pandas>=2.0.0

# Token counting / text utils
tiktoken>=0.6.0

# Logging / debugging (REQUIRED for new features)
rich>=13.7.0

# Retry + robustness (REQUIRED for new features)
tenacity>=8.2.0

# JSON handling improvements
orjson>=3.9.0

# Typing support (helps in bigger projects)
pydantic>=2.5.0

# Optional (for advanced parsing / HTML cleanup)
html5lib>=1.1

# Streamlit (for web interface)
streamlit>=1.28.0

# PDF generation
reportlab>=4.0.0
psycopg2-binary>=2.9.9